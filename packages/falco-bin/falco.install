post_install() {
    printf "#\nTo let falco run you will need to install either:\n"
    printf "* falco-probe-ebpf (ebpf probe)\n"
    printf "* falco-module-dkms (dkms module)\n"
    printf "Or build the probe by your own\n\n"
    printf "For custom falco rules put them in /etc/falco/rules.d\n#\n"
}
